/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: true,
        domains: ['assets.leetcode.com', 'ui-avatars.com'],
    },
};

export default nextConfig;
